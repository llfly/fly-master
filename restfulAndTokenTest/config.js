module.exports = {
    'secret': 'llfly',//used when we create and verify JSON Web Tokens
    'database': 'mongodb://localhost:27017'//the URI with username and password to your MongoDB installation
};